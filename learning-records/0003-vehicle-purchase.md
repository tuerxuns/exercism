# Starting Vehicle Purchase

We started the `vehicle-purchase` exercise, which focuses on conditionals (`if`/`else if`/`else`), comparison operators (`==`, `!=`, `<`, `>`, `<=`, `>=`), and logic operators (`||`, `&&`). The first task is to implement `NeedsLicense`.

## Evidence

- Reviewed the exercise brief and identified the required conditional and comparison patterns.

## Implications

- Builds on basic boolean logic and introduces multi-branch decision structures.
