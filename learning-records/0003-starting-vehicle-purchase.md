# Starting Vehicle Purchase

We started the `vehicle-purchase` exercise, which focuses on conditionals (`if`/`else if`/`else`), comparison operators (`==`, `!=`, `<`, `>`, `<=`, `>=`), and logic operators (`||`, `&&`).

## Details
- Focus: Conditional logic and string/numeric comparisons.
- The first task is to implement `NeedsLicense`.
