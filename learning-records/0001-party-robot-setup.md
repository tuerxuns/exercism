# Starting Party Robot

We started the `party-robot` exercise, which focuses on string formatting (`fmt.Sprintf` with formatting verbs, floats, and zero-padding). The first task is to implement `Welcome`.

## Evidence

- Reviewed the exercise brief and identified the required format verbs (`%s`, `%d`, `%f`) and zero-padding (`%03d`).

## Implications

- Sets up the foundation for practical string formatting with `fmt.Sprintf`.
